type Note @model @auth(rules: [{ allow: public }]) {
  id: ID!
    @index(
      name: "RequestByAddress"
      queryField: "RequestByAddress"
      sortKeyFields: ["address"]
    )
  name: String!
  description: String
  address: String!
  image: String
  createdAt: String!
}
